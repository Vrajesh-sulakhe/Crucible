# Copy this file to .env and fill in real values.
# Never commit .env — it is gitignored.

# baked | live
DEMO_MODE=baked

# Required only when DEMO_MODE=live
OPENAI_API_KEY=

# Shared secret for the demo token gate (live mode)
DEMO_TOKEN=
